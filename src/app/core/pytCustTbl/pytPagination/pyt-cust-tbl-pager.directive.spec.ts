import { PytCustTblPagerDirective } from './pyt-cust-tbl-pager.directive';

describe('PytCustTblPagerDirective', () => {
  it('should create an instance', () => {
    const directive = new PytCustTblPagerDirective();
    expect(directive).toBeTruthy();
  });
});
