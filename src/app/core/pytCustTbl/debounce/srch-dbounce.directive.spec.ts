import { SrchDbounceDirective } from './srch-dbounce.directive';

describe('SrchDbounceDirective', () => {
  it('should create an instance', () => {
    const directive = new SrchDbounceDirective();
    expect(directive).toBeTruthy();
  });
});
