import { ResendOtpDirective } from './resend-otp.directive';

describe('ResendOtpDirective', () => {
  it('should create an instance', () => {
    const directive = new ResendOtpDirective();
    expect(directive).toBeTruthy();
  });
});
